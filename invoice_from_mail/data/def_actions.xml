<?xml version="1.0"?>
<openerp>
    <data noupdate="1">
        <!-- default server action -->
        <record id="invoice_from_mail" model="ir.actions.server">
            <field name="name">Invoice from mail</field>
            <field name="model_id"
                   search="[('model','=','account.invoice')]"/>
            <field name="condition">True</field>
            <field name="type">ir.actions.server</field>
            <field name="state">code</field>
            <field name="code">
vals = self.set_default_dict()
self.create(cr, uid, vals, context=context)
            </field>
        </record>
        <record id="incoming_mail" model="ir.actions.server">
            <field name="name">Incoming mail</field>
            <field name="model_id"
                   search="[('model','=','mail.message')]"/>
            <field name="condition">True</field>
            <field name="type">ir.actions.server</field>
            <field name="state">code</field>
            <field name="code">#</field>
        </record>
    </data>
</openerp>

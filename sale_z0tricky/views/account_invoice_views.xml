<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_invoice_pending" model="ir.actions.act_window">
        <field name="name">Sale Bills</field>
        <field name="res_model">account.invoice</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,kanban,form,calendar,pivot,graph</field>
        <field eval="False" name="view_id"/>
        <field name="domain">[('type','=','out_invoice')]</field>
        <field name="context">{'default_type':'out_invoice', 'type':'out_invoice', 'journal_type': 'sale'}</field>
        <field name="search_view_id" ref="account.view_account_invoice_filter"/>
        <field name="help" type="html">
          <p class="oe_view_nocontent_create">
            Click to create a sale bill.
          </p><p>
            Use this menu to control the invoices to be added to your
            customers. When registering a new bill, set the sale order
            and Odoo will fill the bill automatically according to ordered
            or received quantities.
          </p>
        </field>
    </record>

    <record id="sale_open_invoice" model="ir.actions.act_window">
        <field name="name">Sale Bills</field>
        <field name="res_model">account.invoice</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,kanban,form,calendar,pivot,graph</field>
        <field eval="False" name="view_id"/>
        <field name="domain">[('type','in',('out_invoice', 'out_refund')),('sale_id','=',active_id)]</field>
        <field name="context">{'default_type': 'out_invoice', 'type': 'out_invoice', 'journal_type': 'sale', 'default_sale_id': active_id}</field>
        <field name="search_view_id" ref="account.view_account_invoice_filter"/>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
            Click to record a sale bill related to this sale.
            </p>
            <p>
            You can control the invoice to your customer according to
            what you sold (services) or sold (products).
            </p>
        </field>
    </record>
    <record id="action_invoice_supplier_tree1_view1" model="ir.actions.act_window.view">
        <field eval="1" name="sequence"/>
        <field name="view_mode">tree</field>
        <field name="view_id" ref="account.invoice_tree"/>
        <field name="act_window_id" ref="sale_open_invoice"/>
    </record>
    <record id="action_invoice_tree1_view2" model="ir.actions.act_window.view">
        <field eval="2" name="sequence"/>
        <field name="view_mode">form</field>
        <field name="view_id" ref="account.invoice_form"/>
        <field name="act_window_id" ref="sale_open_invoice"/>
    </record>

    <record id="view_invoice_customer_sale_form" model="ir.ui.view">
        <field name="name">account.invoice.customer.sale</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_form"/>
        <field name="arch" type="xml">
            <field name="number" position="after" >
                 <field name="sale_id" attrs="{'readonly': [('state','not in',['draft'])], 'invisible': ['|', ('state', '=', 'sale'), ('type', '=', 'out_refund')]}" class="oe_edit_only"
                    options="{'no_create': True}"/>
            </field>
            <xpath expr="//field[@name='invoice_line_ids']/tree/field[@name='company_id']" position="after">
                <field name="sale_line_id" invisible="1"/>
                <field name="sale_id" invisible="1"/>
            </xpath>
        </field>
    </record>

    <record id="view_invoice_line_form_inherit_sale" model="ir.ui.view">
        <field name="name">account.invoice.line.form.inherit.sale</field>
        <field name="model">account.invoice.line</field>
        <field name="inherit_id" ref="account.view_invoice_line_form"/>
        <field name="arch" type="xml">
            <field name="account_id" position="before">
                <field name="sale_id" invisible="context.get('type') in ('in_invoice', 'in_refund')"/>
            </field>
        </field>
    </record>
</odoo>
